import reducer from './reducers';

export { default as heroTypes } from './types';
export { default as heroSagas } from './sagas';
export { default as heroActions } from './actions';
export { default as heroSelectors } from './selectors';

export default reducer;
